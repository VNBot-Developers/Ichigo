/* jshint ignore:start */
module.exports = (guild, bot) => {
    guilddefault = guild.defaultChannel;
    guilddefault.send(`Hi, I'm ${bot.user.username}!
You just added my to your server :3 so how to use me ? here are some things you should know :

type `)
};